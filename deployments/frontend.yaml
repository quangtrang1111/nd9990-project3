apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-frontend
  labels:
    service: udagram-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      service: udagram-frontend
  template:
    metadata:
      labels:
        service: udagram-frontend
        api: external
    spec:
      containers:
      - image: quangtrang1111/udagram-frontend
        name: udagram-frontend
        ports:
        - containerPort: 80
        imagePullPolicy: Always
      restartPolicy: Always
